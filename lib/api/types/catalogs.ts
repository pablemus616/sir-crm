export interface Sector {
  id: number;
  name: string;
  active: boolean;
}

export interface PositionArea {
  id: number;
  name: string;
  active: boolean;
}

export interface ContactType {
  id: number;
  name: string;
}

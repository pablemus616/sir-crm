'use client';

// TODO Task 5.4: implement the create opportunity form dialog

export function OpportunityCreateForm({
  open: _open,
  onOpenChange: _onOpenChange,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}) {
  return null;
}
